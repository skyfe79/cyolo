# Code Review Discussion: task-001
Started: 2026-04-17T19:20:00Z
Task: Extend DietOptions and parse_diet_args() with stale/cache/profile/all flags

---
## Review Round 1

### Codex (review)
VERDICT: CHANGES_REQUESTED
FEEDBACK:
- `src/diet.rs:744` `--profile` currently accepts the next token unconditionally, so `cyolo diet --profile --apply` is parsed as `profile=Some("--apply")` instead of erroring for a missing profile value. This swallows following flags and violates the expected CLI error handling. Reject flag-looking values for `--profile` and add a regression test for cases like `--profile --apply` / `--profile --all`.

### Claude (fix applied)
- Added `|| args[i].starts_with("--")` guard to `--profile` value check
- Added regression tests: `test_parse_args_profile_swallows_flag`, `test_parse_args_profile_swallows_all_flag`
- All 80 diet tests pass

---
## Review Round 2

### Codex (re-review)
VERDICT: APPROVED

## Result
Status: APPROVED
