# Code Review Discussion: task-004
Started: 2026-04-17T10:52:19Z
Task: Implement resolve_target_profiles() for multi-profile diet targeting

---
## Review Round 1

### Codex (review)
VERDICT: CHANGES_REQUESTED
FEEDBACK:
- Expand `profile.config_dir` before returning it in `resolve_target_profiles()` for both `--profile` and `--all`. `CyoloConfig::load()` deserializes `PathBuf` verbatim, so a registered profile stored as `"~/..."` will currently be returned unexpanded.
- Add isolated tests for `--profile <existing>`, `--all` with registered profiles, and `--all` with no profiles.

### Claude (applied)
- Added `expand_tilde()` import and applied to both `--profile` and `--all` paths.
- Extracted `resolve_profiles_from_config()` for testability.
- Added 5 isolated tests: profile_found, profile_not_found, all_with_profiles, all_no_profiles, tilde_expansion.

---
## Review Round 2

### Codex (re-review)
VERDICT: APPROVED

## Result
Status: APPROVED
