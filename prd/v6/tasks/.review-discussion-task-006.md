# Code Review Discussion: task-006
Started: 2026-04-17
Task: Implement dispatch() for diet argument parsing and pipeline orchestration

---
## Review Round 1

### Codex (review)
VERDICT: CHANGES_REQUESTED
FEEDBACK:
- `src/diet.rs:584-595` is incorrect for mixed arguments: `parse_diet_args()` returns immediately on the first `--apply`, so `cyolo diet --apply --unknown` is accepted instead of rejected. Validate the full arg list before returning, and add a regression test for the `--apply`-then-unknown case.
- `src/diet.rs:639-642` reports success too early: `print_report(&report, options.apply)` prints the `"Cleanup complete."` footer before `apply()` has actually succeeded. If `apply()` fails during backup or deletion, the user still sees a success message. Only emit the applied/success footer after `apply()` returns `Ok(())`.

### Claude (fixes applied)
1. Fixed `parse_diet_args()` to use a `mut apply` flag and iterate all args before returning. Added `test_parse_args_apply_then_unknown` regression test.
2. Fixed `dispatch()` to always call `print_report(&report, false)` (dry-run footer), then only `println!("Cleanup complete.")` after `apply()` returns `Ok`.

---
## Review Round 2

### Codex (re-review)
VERDICT: APPROVED

## Result
Status: APPROVED
