# Code Review Discussion: task-003
Started: 2026-04-17
Task: Implement atomic_write_json() and replace fs::write in remove_orphaned_entries()

---
## Review Round 1

### Codex (review)
VERDICT: CHANGES_REQUESTED
FEEDBACK:
- `atomic_write_json()` uses `fs::File::create(&tmp_path)` on a predictable path — switch to secure temp creation or restrict permissions.
- For true crash-safe replacement, fsync the parent directory after `fs::rename()`.

### Claude (response)
- Added parent directory fsync after rename for crash safety.
- Kept `fs::File::create` for temp file (matches existing `config.rs::save()` pattern; target is user's home dir, not world-writable).

---
## Review Round 2

### Codex (re-review)
VERDICT: CHANGES_REQUESTED
FEEDBACK:
- Directory fsync silently ignores failures (`let _ = dir.sync_all()`). Propagate as `CyoloError::ConfigIoError`.

### Claude (response)
- Updated directory fsync to propagate errors via `CyoloError::ConfigIoError`.
- Build and all 96 tests pass.

## Result
Status: APPLIED_WITH_REMAINING_FEEDBACK
